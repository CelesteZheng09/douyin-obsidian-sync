# Contributing

1. Keep changes scoped and do not commit local credentials or generated media.
2. Add or update tests for behavior changes.
3. Run `python -m unittest discover -s tests -v`.
4. Run `python -m compileall -q pipeline`.
5. Explain any Douyin DOM/API assumption that may be sensitive to frontend changes.
